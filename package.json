{
  "scripts": {
    "dev": "node smol-abell/dev-server.js",
    "build:server": "vite build --outDir dist/server --ssr src/entry-server.js",
    "generate": "NODE_ENV=production npm run build:server && node smol-abell/generate.js"
  },
  "dependencies": {
    "abell-renderer": "^1.0.0-alpha.3",
    "express": "^4.17.1",
    "vite": "^2.6.14"
  }
}
